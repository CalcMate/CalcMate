# Contract Schema Registry — App Factory 자동 관리
# save_app() / delete_app() 경유로만 갱신됨. 직접 편집 금지.
# instances 키: {calc_slug: {contract_slug, generated_at, formula_status, test_cases_status}}
instances: {}
